@model Slider
<div class="container">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">@Model.Title1</h5>
                    <h6 class="card-subtitle mb-2 text-muted">@Model.Title2</h6>
                    <a asp-action="index" class="card-link btn btn-primary ">Go to List</a>
                    <form asp-action="delete" method="post" class="d-inline-block">
                        <input type="hidden" name="Id" value="@Model.Id" />
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

